[package]
name = "intermededit"
version = "0.1.0"
edition = "2024"

[dependencies]
derive_more = { version="2.0.1", features=["into", "deref", "from", "as_ref", "display"] }
indicatif = "0.17.11"
itertools = "0.14.0"
rayon = "1.10.0"

[features]
cache-letters = []
