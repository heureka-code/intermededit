(Some docs are still on the other branch)

This can be run with [Nix](https://nixos.wiki/wiki/Nix_package_manager) and [Just](https://github.com/casey/just)

```shell
nix develop
```
(or `nix develop .#full` if you don't have [Rust](https://rust-lang.org/) installed)

```
just
```
